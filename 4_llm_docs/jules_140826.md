# Grasshopper Script Standardization
- Updated all python scripts in `2_grasshopper_scripts/0_scripts` to adhere to the component metadata standard specified in `ScriptStandards.md`.
- Ensure all input and output descriptions are documented using `ghenv.Component.Params.Input` and `Output` properties inside a `try: ... except NameError: pass` block.
- Standardized whitespace and removed trailing spaces inside Python components.
- Updated `3_web/component_lib.csv` entries to correctly reflect the updated python python script component metadata properties.
